extends ../layout.pug
include ../mixins/custompage.pug

block head
	title #{__('Pages')}

block content
	h1.board-title #{__('Pages')}
	include ../includes/stickynav.pug
	if custompages && custompages.length > 0
		.table-container.flex-center.mv-11
			table.table-body
				tr.table-row
					td
						each page in custompages
							+custompage(page)
	else
		p #{__('Site has no pages yet.')}